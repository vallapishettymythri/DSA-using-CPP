#include <iostream>
using namespace std;
int main() {
    int n,d;  //taking input n and d as integer type
    cin >> n >> d; 
    int a[n]; //creating an array of size n
    for(int i=0;i<n;i++){
        cin >> a[i]; //taking array as input
    }
    d=d%n;  //d=d%n eg: d=4%5= 4
    for(int i=d;i<n;i++){  //i=4
        cout << a[i] << " "; //prints a[4] which is 5
    }
    for(int i=0;i<d;i++){  //prints from i=0 index to i<d which is i<3 index
        cout << a[i] << " ";
    }
    cout << endl;
    return 0;
}
