#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */ 
    int n;
    cin>> n;
    int a[n];
    for(int i=0;i<n;i++){
        cin >> a[i];
    }
    int currsum=0;
    int maxsum=currsum;
    for(int i=0;i<n;i++){
        currsum+=a[i];
        if(maxsum<currsum){
            maxsum=currsum;
        }
    }
    if(currsum<0){
        currsum=0;
    }
    cout << maxsum << endl;
    return 0;
}
